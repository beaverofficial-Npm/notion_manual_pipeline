# 세션 상태 스냅샷 (2026-07-15) — 컴팩션/맥락유실 대비

> 압축 후 이 파일 먼저 읽고 이어간다. 성빈님(호칭 "성빈님")은 지금 **노션 발행 테스트 중 + 파일 압축 중**.

## 현재 위치 (main = 운영, 배포 완료)
- 브랜치 `feat/ppt-converter-two-way` == origin/main == 운영. 최신 커밋 순서(위가 최신):
  - `d49dd24` perf: 서명 URL 배치화 + 에셋 .in() 청크 (홈10초·tree30초타임아웃 해소)
  - `916dfad` api: 재분석 시 활성 job 가드(큐 폭주 방어)
  - `6730664` worker: 렌더 덱당 1회 원복(이중렌더가 웹 마비시킴)
  - `b77b9a3` group-bake: 크롭용 스트립 렌더(본문 텍스트 픽셀부터 제거) + Dockerfile에 zip 추가
  - `272853e` group-bake: 크롭=이미지박스 그대로(+걸침 합집합) — BOX_CAPTURE_HANDOFF.md 규칙
- 운영 응답: 홈 ~1.5초, tree ~3.5초. DB 비워둠(성빈님이 방금 새 덱 1건 변환).

## 방금 검증된 것 (운영, 148장 덱)
- 변환 **succeeded** ~118초. 슬라이드148/에셋134(이미지박스133). **변환 중 웹 1.5~1.8초 유지**(마비 재현 안 됨).
- 성빈님이 지금 그 결과를 **노션 발행 테스트 중** — 결과 대기.

## 오늘 해결한 문제 (우선순위 = 변환 성공률이 메이저, 웹느림은 하위)
1. 크롭 어중땡이/옆 텍스트 딸림 → **스트립 렌더**(본문 텍스트 XML 제거 후 렌더해 크롭)로 근본 해결.
2. 좀비 로컬 워커(안 끈 poll-loop가 옛 코드로 큐 가로챔) → 종료. **재발방지: 로컬 워커 테스트 후 즉시 kill+보고.**
3. 변환 실패/서버 마비 → 이중 렌더를 1회로 원복 + 큐 원자적 클레임 + 활성 job 가드.
4. **★진짜 근본**: 화면이 슬라이드 장마다 signed URL 순차 생성(O(N)) → 큰 덱서 tree 30초 타임아웃 = "변환/로딩 안 됨"의 핵심. **배치 서명으로 해결.**

## 확정 규칙 (재논의 금지)
- 크롭 = **이미지박스 구간 그대로** + 판에 걸친 요소 합집합(`boxCaptureRect`). 콘텐츠 타이트/우측 클램프는 폐기.
- 크롭용 렌더는 본문 텍스트 shape 제거본(`stripOutsideTextShapes`) — 옆 글자 조각 원천 차단.
- 자동추출(group_bake) **단일 모드**(레거시 capture 웹에서 제거). 번호 마커 원본 보존(1./1)/1-1.). ※ 유의/참고 원문 그대로(콜아웃 변환 안 함). 이미지 위→텍스트 아래. 페이지 경계마다 divider. 발행 이미지 폭 540px.

## ★ 발행(노션) 큰 덱에서 먹통 — 수정·검증·배포 완료 (2026-07-15, 커밋 24b385d)
- 증상(해결됨): 148장/이미지134 덱 발행 시 "로딩 돌다 팝업 꺼짐". publish_run 이 ~1분 후 status=cancelled(=클라이언트 abort/연결끊김).
- 원인: 발행이 이미지를 한 스트리밍 요청 안에서 순차 업로드 → 진행신호 공백이 ~60초 프록시 idle 타임아웃 넘겨 연결 끊김 → abort → 팝업 닫힘 → 재클릭 누적.
- 수정(main 배포됨):
  - `publish.ts`: 본문 조립 전에 이미지 전량을 `runPool(동시 6)` 병렬 선업로드 + 장당 진행신호(하트비트). 본문 루프는 fileUploadId 참조만. crop 렌더는 renderCache 재사용.
  - `assets.ts`: `notionFetchWithRetry` 429/5xx 재시도(동시 업로드 rate limit 대비).
  - `publish/route.ts`: running 발행(<10분) 있으면 409 거절(중복 누적 차단).
  - `task-review-bake.tsx`: 에러 시 모달 닫지 않고 명확 표시(닫기 버튼)+성공에만 완료/닫기, 발행 중 버튼 잠금.
- 검증(로컬 실측): 그 148장 덱 실제 발행 → **74초 완료**(업로드 55초, 하트비트 ~7초 간격 = 60초 타임아웃 여유). 생성 노션 페이지 **633블록**(h1 8·h2 58·image 134 전부 저장·paragraph 296·divider 136·table 1). 빈 페이지 아님.
- 남은 정석(선택): 발행을 백그라운드 job 화(변환처럼 큐+워커+폴링 — 연결 끊겨도 지속). 지금 수정으로 실동작하나, 더 튼튼히 하려면 구조 브랜치에서.

## 미결 / 다음 (성빈님 결정 대기)
- **50MB 업로드 한도**: Supabase Free 플랜 전역 한도라 코드로 못 올림(버킷 200MB 시도 거부됨). Pro 업그레이드 or PPT 이미지 압축만 가능.
- **구조 보강(성빈님 방침: 새 브랜치+로컬 검증 후 머지, main 얼려둠)**:
  - 진짜 메이저 = "패치오류(fetch failed) 재발 방지". 원인 사슬: 서버 느려짐→업로드 요청 타임아웃→fetch failed→재클릭→악화.
  - (1) **업로드 직행**: 브라우저→우리서버→Supabase 를 브라우저→Supabase 직접(서명URL만 서버가 발급)으로. 업로드가 서버 부하와 무관해짐 = fetch failed 원천 차단. ★핵심
  - (2) **워커 분리**: Railway에 변환 워커를 별도 서비스로(코드는 이미 poll-loop 독립, 인프라만 분리). 변환이 웹 CPU 안 먹게.
  - (3) 프론트: 업로드 중복클릭 잠금 + 타임아웃 시 명확한 혼잡 안내.
  - 순서: (1)+(3) 코드만 먼저 → (2) 인프라는 성빈님 확인. **전부 새 브랜치에서 로컬 검증 후 main 머지.**

## 운영 규율 (오늘 사달의 교훈)
- 검증 안 된 것 main(운영) 직접 push 금지 → 새 브랜치+로컬 검증.
- 로컬 워커/dev 띄우면 반드시 종료 + 보고(좀비 방지).
- 판정 기준 = **변환 성공 여부**(웹속도는 부가). 됐다는 말은 실측 후에만.
